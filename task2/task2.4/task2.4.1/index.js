const rx = require('rxjs');
const fs = require('fs');

// Task 2.4.1
// Retrieve context from the given CoreSubscriber.
// Use given key in order to access value stored in the Context
// Use Mono.deferContextual(lambda) in order to access a ContextView.
// Use ContextView#get(key)
